<launch>
    <!-- Benchmark Parameters -->
    <arg name="planner" default="astar" />
    <arg name="goal_x" default="36.0" />
    <arg name="goal_y" default="32.0" />
    <arg name="output_dir" default="$(find plannie)/results" />
    <arg name="summary_file" default="" />
    <arg name="target_startup_time" default="0.0" />
    <arg name="max_timeout" default="180.0" />
    <arg name="seed" default="0" />

    <!-- Benchmark Manager Node -->
    <!-- Start Simulation (Using existing launch from internal packages via launch_simulator.sh logic but directly) -->
    <!-- The launch_simulator.sh script sets up user_config and then launches a tailored launch file. 
         Ideally, we should rely on launch_simulator.sh to start the sim, then launch this benchmark. 
         But to automate, we can try to include a simplified launch or assume sim is running. 
         
         For now, let's assume this launch file STARTS the benchmark node. 
         The user typically launches sim first. 
    -->

    <node pkg="plannie" type="benchmark_manager.py" name="benchmark_manager" output="screen">
        <param name="output_dir" value="$(arg output_dir)" />
        <param name="planner_name" value="$(arg planner)" />
        <param name="goal_x" value="$(arg goal_x)" />
        <param name="goal_y" value="$(arg goal_y)" />
        <param name="summary_file" value="$(arg summary_file)" />
        <param name="target_startup_time" value="$(arg target_startup_time)" />
        <param name="max_timeout" value="$(arg max_timeout)" />
        <param name="seed" value="$(arg seed)" />
    </node>
</launch>
